# FRONTEND.md

Reviewed on 2026-03-07.

`mangarr` has no frontend today.

If a UI is added later:

- keep CLI as the source of truth for core workflows
- document the UI's relationship to local config and downloaded archives
- avoid inventing a dashboard unless it materially improves operator visibility
- add product specs and design docs before shipping UI complexity
